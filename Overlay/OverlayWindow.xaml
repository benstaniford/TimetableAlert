<Window x:Class="TimetableAlert.Overlay.OverlayWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Timetable Alert"
        SizeToContent="Height"
        Width="720"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        Focusable="False"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize">
    <Border Margin="16"
            Padding="32,20"
            CornerRadius="14"
            Background="#F0121216"
            BorderBrush="#FFFF3B30"
            BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect Color="#FF000000" BlurRadius="24" ShadowDepth="0" Opacity="0.8"/>
        </Border.Effect>
        <StackPanel>
            <TextBlock x:Name="SubjectText"
                       Text="LESSON"
                       Foreground="#FFFF3B30"
                       FontFamily="Segoe UI Variable Display, Segoe UI"
                       FontSize="40"
                       FontWeight="Bold"
                       TextWrapping="Wrap"
                       TextAlignment="Center"/>
            <TextBlock x:Name="TimingText"
                       Text="starting soon"
                       Margin="0,2,0,0"
                       Foreground="#FFFF6B60"
                       FontFamily="Segoe UI Variable Text, Segoe UI"
                       FontSize="26"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"
                       TextAlignment="Center"/>
            <TextBlock x:Name="DetailText"
                       Text=""
                       Margin="0,8,0,0"
                       Foreground="#FFD9938E"
                       FontFamily="Segoe UI Variable Text, Segoe UI"
                       FontSize="18"
                       TextWrapping="Wrap"
                       TextAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>
